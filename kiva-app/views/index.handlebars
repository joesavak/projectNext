<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title">
            Most Recent Loans
        </h3>
    </div>
    <div class="panel-body">
        {{#each loans}}
            <div class="col-md-4 text-center" style="padding-bottom: 1em;">{{name}} from {{location.country}} is requesting {{loan_amount}} {{use}}</div>
        {{/each}}
    </div>
</div>